/* main.c */
#include <stdio.h>
#include <string.h>
/*
*main - start
*allocate space for string
*type the string
*/
int main(void)
{
	char str[100];
		strcpy(str, "\"Programming is like building a multilingual puzzle");
	puts(str);
	return (0);
}
