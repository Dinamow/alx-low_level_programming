#include "main.h"
/**
 * rev_string - start point
 * @s: input
 * Return always 0 (success)
*/
void rev_string(char *s)
{
int i;
int chars;
char s[100];
for (i = 0; s[i] != '\0'; i++)
chars = i;
for (i = 0; chars != -1; chars--)
s0[i] = s[chars];
for (i = 0; i <= chars; i++)
s[i] = s0[i];
}
