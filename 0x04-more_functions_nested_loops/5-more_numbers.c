#include "main.h"
 /**
  * more_numbers - start point
  * 2 loops
  * print
  * Return always 0(success)
 */

void more_numbers(void)
{
int i, j;
for(i = 0; i < 9; i++)
    for(j = 0; j < 15; j++)
    _putchar(j + 48);
_putchar('\n');

}
