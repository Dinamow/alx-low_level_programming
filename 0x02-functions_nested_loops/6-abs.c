#include "main.h"
#include <unistd.h>
/**
 * _abs - start point
 * take input
 * return the absolute value of input
*/
int _abs(int)
{ 
int i;
if (i > 0)
{
return (i);
}
else
{
i = i * -1;
return (i);
}

}
